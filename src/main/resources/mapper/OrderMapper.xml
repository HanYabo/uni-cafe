<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.han.cafe.mapper.OrderMapper">
    
    <resultMap id="AdminOrderDetailMap" type="com.han.cafe.vo.AdminOrderDetailVO">
        <id property="orderId" column="order_id"/>
        <result property="userId" column="user_id"/>
        <result property="nickname" column="nickname"/>
        <result property="mobile" column="mobile"/>
        <result property="status" column="status"/>
        <result property="payType" column="pay_type"/>
        <result property="payTime" column="pay_time"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="discountAmount" column="discount_amount"/>
        <result property="payAmount" column="pay_amount"/>
        <result property="couponId" column="coupon_id"/>
        <result property="remark" column="remark"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <collection property="items" ofType="com.han.cafe.vo.AdminOrderDetailVO$OrderItemDetail">
            <id property="itemId" column="item_id"/>
            <result property="productId" column="product_id"/>
            <result property="productName" column="product_name"/>
            <result property="basePrice" column="base_price"/>
            <result property="actualPrice" column="actual_price"/>
            <result property="quantity" column="quantity"/>
            <result property="subtotal" column="subtotal"/>
            <collection property="specs" ofType="com.han.cafe.vo.AdminOrderDetailVO$OrderItemSpecDetail">
                <result property="specId" column="spec_id"/>
                <result property="specName" column="spec_name"/>
                <result property="specValueId" column="spec_value_id"/>
                <result property="specValue" column="spec_value"/>
                <result property="extraPrice" column="extra_price"/>
            </collection>
        </collection>
    </resultMap>

    <select id="selectAdminOrderPage" resultMap="AdminOrderDetailMap">
        SELECT 
            o.order_id, o.user_id, u.nickname, u.mobile,
            o.status, o.pay_type, o.pay_time,
            o.total_amount, o.discount_amount, o.pay_amount,
            o.coupon_id, o.remark, o.created_at, o.updated_at,
            oi.item_id, oi.product_id, oi.product_name,
            oi.base_price, oi.actual_price, oi.quantity, oi.subtotal,
            ois.spec_id, ois.spec_name, ois.spec_value_id,
            ois.spec_value, ois.extra_price
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        LEFT JOIN order_item oi ON o.order_id = oi.order_id
        LEFT JOIN order_item_spec ois ON oi.item_id = ois.item_id
        <where>
            o.is_deleted = 0
            <if test="status != null">
                AND o.status = #{status}
            </if>
        </where>
        ORDER BY o.created_at DESC
    </select>

    <select id="selectAdminOrderDetail" resultMap="AdminOrderDetailMap">
        SELECT 
            o.order_id, o.user_id, u.nickname, u.mobile,
            o.status, o.pay_type, o.pay_time,
            o.total_amount, o.discount_amount, o.pay_amount,
            o.coupon_id, o.remark, o.created_at, o.updated_at,
            oi.item_id, oi.product_id, oi.product_name,
            oi.base_price, oi.actual_price, oi.quantity, oi.subtotal,
            ois.spec_id, ois.spec_name, ois.spec_value_id,
            ois.spec_value, ois.extra_price
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.user_id
        LEFT JOIN order_item oi ON o.order_id = oi.order_id
        LEFT JOIN order_item_spec ois ON oi.item_id = ois.item_id
        WHERE o.order_id = #{orderId} AND o.is_deleted = 0
    </select>
</mapper> 